---
---

<section class="services" id="services">
  <div class="container">
    <div class="services-header">
      <h2>Beyond the phone</h2>
      <p>We deliver what your restaurant actually needs.</p>
    </div>

    <div class="services-grid">
      <div class="service-card featured">
        <div class="service-badge">Core Service</div>
        <h3>Virtual AI Host</h3>
        <p class="service-tagline">Reservation & call handling that pays for itself.</p>
        <ul class="service-features">
          <li>Answers all inbound calls instantly</li>
          <li>Books, modifies, and cancels reservations</li>
          <li>Handles FAQs, escalates edge cases</li>
          <li>Works peak hours, after-hours, holidays</li>
        </ul>
        <div class="service-roi">Prove ROI in days with before/after call logs.</div>
      </div>

      <div class="service-card">
        <h3>Online Order Automation</h3>
        <p class="service-tagline">Capture more takeout revenue.</p>
        <ul class="service-features">
          <li>Accept orders via website or social</li>
          <li>Auto-send to POS/kitchen</li>
          <li>Smart upsells on every order</li>
          <li>Reduce errors from busy staff</li>
        </ul>
      </div>

      <div class="service-card">
        <h3>SMS & Email Confirmations</h3>
        <p class="service-tagline">Fewer no-shows. Zero effort.</p>
        <ul class="service-features">
          <li>Automatic reservation confirmations</li>
          <li>Reminder texts before arrival</li>
          <li>Post-visit thank you & feedback</li>
          <li>Reduces no-shows immediately</li>
        </ul>
      </div>

      <div class="service-card">
        <h3>Analytics & Insights</h3>
        <p class="service-tagline">Know what's working.</p>
        <ul class="service-features">
          <li>Peak hours & missed opportunity data</li>
          <li>Repeat customer tracking</li>
          <li>Staffing & menu trend predictions</li>
          <li>Premium add-on after core ROI</li>
        </ul>
      </div>

      <div class="service-card">
        <h3>Branding & Website</h3>
        <p class="service-tagline">Look as good as you are.</p>
        <ul class="service-features">
          <li>Website redesign or rebuild</li>
          <li>Menu design & photography</li>
          <li>Brand identity refresh</li>
          <li>Whatever you need—just ask</li>
        </ul>
      </div>
    </div>

    <div class="mission">
      <p>
        We're not here to replace your team. We're here to make their lives easier—so they can focus on what matters most: your guests.
      </p>
    </div>
  </div>
</section>

<style>
  .services {
    padding: var(--space-4xl) 0;
    background: var(--color-bg);
  }

  .services-header {
    text-align: center;
    max-width: 500px;
    margin: 0 auto var(--space-3xl);
  }

  .services-header h2 {
    margin-bottom: var(--space-sm);
  }

  .services-header p {
    color: var(--color-text-light);
    font-size: 1.125rem;
    margin: 0;
  }

  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    margin-bottom: var(--space-3xl);
  }

  .service-card {
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
    padding: var(--space-xl);
    position: relative;
    transition: all var(--transition-base);
  }

  .service-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
  }

  .service-card.featured {
    background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
    color: white;
    grid-column: 1 / -1;
  }

  .service-badge {
    display: inline-block;
    padding: var(--space-xs) var(--space-sm);
    background: rgba(255, 255, 255, 0.2);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: var(--radius-full);
    margin-bottom: var(--space-md);
  }

  .service-card h3 {
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
  }

  .service-card.featured h3 {
    color: white;
    font-size: 1.5rem;
  }

  .service-tagline {
    color: var(--color-text-light);
    margin-bottom: var(--space-lg);
    font-size: 0.9375rem;
  }

  .service-card.featured .service-tagline {
    color: rgba(255, 255, 255, 0.8);
  }

  .service-features {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .service-features li {
    position: relative;
    padding-left: 1.5rem;
    margin-bottom: var(--space-sm);
    font-size: 0.9375rem;
    color: var(--color-text-light);
  }

  .service-card.featured .service-features li {
    color: rgba(255, 255, 255, 0.9);
  }

  .service-features li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5rem;
    width: 6px;
    height: 6px;
    background: var(--color-secondary);
    border-radius: 50%;
  }

  .service-card.featured .service-features li::before {
    background: rgba(255, 255, 255, 0.6);
  }

  .service-roi {
    margin-top: var(--space-lg);
    padding-top: var(--space-lg);
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    font-size: 0.875rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.9);
  }

  .mission {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
    padding: var(--space-2xl);
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
  }

  .mission p {
    color: var(--color-text-light);
    font-size: 1.125rem;
    font-style: italic;
    margin: 0;
  }

  @media (min-width: 768px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .service-card.featured {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-xl);
      align-items: start;
    }

    .service-card.featured .service-badge,
    .service-card.featured h3,
    .service-card.featured .service-tagline {
      grid-column: 1 / -1;
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(4, 1fr);
    }

    .service-card.featured {
      grid-column: span 2;
      display: block;
    }
  }
</style>

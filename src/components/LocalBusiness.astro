---
---

<section class="local-business">
  <div class="container">
    <div class="local-grid">
      <div class="local-content">
        <span class="badge">Built for Real Restaurants</span>
        <h2>Designed for Kitchens, Not Tech Teams</h2>
        <p class="local-intro">
          You run a restaurant, not a software company. MSN AI fits into your existing workflow
          with minimal disruption—no complicated setup, no learning curve.
        </p>

        <div class="simplicity-list">
          <div class="simplicity-item">
            <div class="simplicity-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22 4 12 14.01 9 11.01"/>
              </svg>
            </div>
            <div class="simplicity-content">
              <strong>Works with your current phone system</strong>
              <p>No new hardware. No new numbers. Just smarter call handling.</p>
            </div>
          </div>

          <div class="simplicity-item">
            <div class="simplicity-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22 4 12 14.01 9 11.01"/>
              </svg>
            </div>
            <div class="simplicity-content">
              <strong>Syncs with your reservation book</strong>
              <p>Whether you use paper, a spreadsheet, or booking software—we integrate.</p>
            </div>
          </div>

          <div class="simplicity-item">
            <div class="simplicity-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22 4 12 14.01 9 11.01"/>
              </svg>
            </div>
            <div class="simplicity-content">
              <strong>Setup in days, not weeks</strong>
              <p>We handle everything. You approve the script and go live.</p>
            </div>
          </div>

          <div class="simplicity-item">
            <div class="simplicity-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22 4 12 14.01 9 11.01"/>
              </svg>
            </div>
            <div class="simplicity-content">
              <strong>Staff stays in control</strong>
              <p>Your team can always take over. MSN AI assists—never replaces.</p>
            </div>
          </div>
        </div>
      </div>

      <div class="local-visual">
        <div class="workflow-card">
          <h3>Your Workflow, Unchanged</h3>
          <div class="workflow-steps">
            <div class="workflow-step">
              <div class="step-number">1</div>
              <div class="step-content">
                <strong>We learn your business</strong>
                <p>Menu, hours, policies, voice</p>
              </div>
            </div>
            <div class="workflow-step">
              <div class="step-number">2</div>
              <div class="step-content">
                <strong>You approve the script</strong>
                <p>Nothing goes live without your OK</p>
              </div>
            </div>
            <div class="workflow-step">
              <div class="step-number">3</div>
              <div class="step-content">
                <strong>Calls start flowing</strong>
                <p>Reservations appear in your system</p>
              </div>
            </div>
            <div class="workflow-step">
              <div class="step-number">4</div>
              <div class="step-content">
                <strong>You focus on guests</strong>
                <p>We handle the phones</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .local-business {
    background: white;
  }

  .local-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  .badge {
    display: inline-block;
    padding: var(--space-xs) var(--space-md);
    background: var(--color-secondary);
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: var(--radius-full);
    margin-bottom: var(--space-md);
  }

  .local-content h2 {
    margin-bottom: var(--space-lg);
  }

  .local-intro {
    font-size: 1.125rem;
    color: var(--color-text-light);
    margin-bottom: var(--space-xl);
  }

  .simplicity-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .simplicity-item {
    display: flex;
    gap: var(--space-md);
  }

  .simplicity-icon {
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-success);
    border-radius: var(--radius-full);
    color: white;
    flex-shrink: 0;
  }

  .simplicity-content strong {
    display: block;
    margin-bottom: var(--space-xs);
    color: var(--color-text);
  }

  .simplicity-content p {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    margin: 0;
  }

  .workflow-card {
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
    padding: var(--space-xl);
    border: 1px solid var(--color-border);
  }

  .workflow-card h3 {
    margin-bottom: var(--space-xl);
    text-align: center;
  }

  .workflow-steps {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .workflow-step {
    display: flex;
    gap: var(--space-md);
    align-items: flex-start;
  }

  .step-number {
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary);
    color: white;
    font-weight: 700;
    border-radius: var(--radius-full);
    flex-shrink: 0;
  }

  .step-content {
    flex: 1;
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--color-border);
  }

  .workflow-step:last-child .step-content {
    border-bottom: none;
    padding-bottom: 0;
  }

  .step-content strong {
    display: block;
    margin-bottom: var(--space-xs);
    color: var(--color-text);
  }

  .step-content p {
    color: var(--color-text-light);
    font-size: 0.875rem;
    margin: 0;
  }

  @media (min-width: 1024px) {
    .local-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

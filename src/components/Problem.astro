---
---

<section class="problem" id="problem">
  <div class="container">
    <div class="problem-content">
      <h2>Every missed call is a table<br /><span class="text-gradient">that went somewhere else.</span></h2>
    </div>

    <div class="stats-row">
      <div class="stat-card" data-answer="70%">
        <div class="stat-question">What % of callers won't try again?</div>
        <div class="stat-reveal">
          <span class="stat-number">70%</span>
          <span class="stat-label">gone forever</span>
        </div>
      </div>

      <div class="stat-card" data-answer="$4,000+">
        <div class="stat-question">Monthly revenue lost to missed calls?</div>
        <div class="stat-reveal">
          <span class="stat-number">$4,000+</span>
          <span class="stat-label">every month</span>
        </div>
      </div>

      <div class="stat-card" data-answer="Peak hours">
        <div class="stat-question">When do you miss the most?</div>
        <div class="stat-reveal">
          <span class="stat-number">6-9 PM</span>
          <span class="stat-label">peak dinner rush</span>
        </div>
      </div>
    </div>

    <p class="problem-hint">Hover to reveal</p>
  </div>
</section>

<style>
  .problem {
    padding: var(--space-4xl) 0;
    background: var(--color-bg);
  }

  .problem-content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-3xl);
  }

  .problem-content h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
  }

  .stats-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    max-width: 900px;
    margin: 0 auto;
  }

  .stat-card {
    position: relative;
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
    padding: var(--space-2xl);
    text-align: center;
    cursor: pointer;
    overflow: hidden;
    transition: all var(--transition-base);
    border: 2px solid transparent;
  }

  .stat-card:hover {
    border-color: var(--color-secondary);
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(99, 102, 241, 0.15);
  }

  .stat-question {
    font-size: 1.125rem;
    font-weight: 500;
    color: var(--color-text);
    transition: all var(--transition-base);
  }

  .stat-card:hover .stat-question {
    opacity: 0;
    transform: translateY(-10px);
  }

  .stat-card:hover {
    background: white;
  }

  .stat-question {
    transition: all var(--transition-base);
  }

  .stat-card:hover .stat-question {
    color: var(--color-text-muted);
  }

  .stat-reveal {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
    transition: all var(--transition-base);
    text-align: center;
  }

  .stat-card:hover .stat-reveal {
    opacity: 1;
  }

  .stat-number {
    display: block;
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-secondary);
    margin-bottom: var(--space-xs);
  }

  .stat-label {
    font-size: 0.9375rem;
    color: var(--color-text-light);
  }

  .problem-hint {
    text-align: center;
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    margin-top: var(--space-lg);
    margin-bottom: 0;
  }

  @media (min-width: 768px) {
    .stats-row {
      grid-template-columns: repeat(3, 1fr);
    }

    .stat-card {
      padding: var(--space-2xl) var(--space-xl);
    }
  }
</style>

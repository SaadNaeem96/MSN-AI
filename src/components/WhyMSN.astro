---
---

<section class="services" id="services">
  <div class="container">
    <div class="services-header">
      <h2>Everything your restaurant needs</h2>
      <p>Handle customer service end-to-end. We only escalate what matters.</p>
    </div>

    <div class="services-grid">
      <div class="service-card">
        <div class="service-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
          </svg>
        </div>
        <h3>Virtual AI Host</h3>
        <p>Answers calls, books reservations, handles FAQs. Works 24/7.</p>
        <span class="service-tag">Core</span>
      </div>

      <div class="service-card">
        <div class="service-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="9" cy="21" r="1"/>
            <circle cx="20" cy="21" r="1"/>
            <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/>
          </svg>
        </div>
        <h3>Order Automation</h3>
        <p>Accept online orders, auto-send to kitchen, smart upsells.</p>
        <span class="service-tag">Revenue</span>
      </div>

      <div class="service-card">
        <div class="service-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
          </svg>
        </div>
        <h3>SMS & Email</h3>
        <p>Confirmations, reminders, follow-ups. Reduce no-shows.</p>
        <span class="service-tag">Retention</span>
      </div>

      <div class="service-card">
        <div class="service-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="20" x2="18" y2="10"/>
            <line x1="12" y1="20" x2="12" y2="4"/>
            <line x1="6" y1="20" x2="6" y2="14"/>
          </svg>
        </div>
        <h3>Analytics</h3>
        <p>Peak hours, missed calls, trends. Know what's working.</p>
        <span class="service-tag">Insights</span>
      </div>

      <div class="service-card">
        <div class="service-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polygon points="12 2 2 7 12 12 22 7 12 2"/>
            <polyline points="2 17 12 22 22 17"/>
            <polyline points="2 12 12 17 22 12"/>
          </svg>
        </div>
        <h3>Branding & Web</h3>
        <p>Website, menu design, brand refresh. Look your best.</p>
        <span class="service-tag">Growth</span>
      </div>
    </div>

    <div class="mission">
      <p>
        We're not here to replace your team—we're here to let them focus on what matters: <strong>your guests.</strong>
      </p>
    </div>
  </div>
</section>

<style>
  .services {
    padding: var(--space-4xl) 0;
    background: var(--color-bg-alt);
  }

  .services-header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-3xl);
  }

  .services-header h2 {
    margin-bottom: var(--space-sm);
  }

  .services-header p {
    color: var(--color-text-light);
    font-size: 1.125rem;
    margin: 0;
  }

  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    margin-bottom: var(--space-3xl);
  }

  .service-card {
    position: relative;
    background: white;
    border-radius: var(--radius-xl);
    padding: var(--space-2xl) var(--space-xl);
    transition: all var(--transition-base);
    border: 2px solid transparent;
    cursor: default;
    min-height: 220px;
  }

  .service-card:hover {
    border-color: var(--color-secondary);
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(99, 102, 241, 0.12);
  }

  .service-card:hover .service-icon {
    background: var(--color-secondary);
    color: white;
  }

  .service-icon {
    width: 3rem;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(99, 102, 241, 0.1);
    border-radius: var(--radius-lg);
    color: var(--color-secondary);
    margin-bottom: var(--space-md);
    transition: all var(--transition-base);
  }

  .service-card h3 {
    font-size: 1.125rem;
    margin-bottom: var(--space-sm);
  }

  .service-card p {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    margin: 0;
    line-height: 1.5;
  }

  .service-tag {
    position: absolute;
    top: var(--space-lg);
    right: var(--space-lg);
    padding: var(--space-xs) var(--space-sm);
    background: rgba(99, 102, 241, 0.1);
    color: var(--color-secondary);
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: var(--radius-full);
  }

  .mission {
    text-align: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .mission p {
    color: var(--color-text-light);
    font-size: 1.0625rem;
    margin: 0;
  }

  .mission strong {
    color: var(--color-text);
  }

  @media (min-width: 640px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(5, 1fr);
    }

    .service-card {
      padding: var(--space-2xl) var(--space-lg);
      min-height: 240px;
    }
  }
</style>

---
---

<section class="problem" id="problem">
  <div class="container">
    <div class="problem-content">
      <h2>While you're serving guests,<br /><span class="text-gradient">your phone keeps ringing.</span></h2>
      <p class="problem-text">
        Every unanswered call is a reservation that went somewhere else.
      </p>
    </div>

    <div class="stats-row">
      <div class="stat-item">
        <span class="stat-number">70%</span>
        <span class="stat-label">of callers won't try again</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat-item">
        <span class="stat-number">$4K+</span>
        <span class="stat-label">lost monthly from missed calls</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat-item">
        <span class="stat-number">Peak hours</span>
        <span class="stat-label">are when you miss the most</span>
      </div>
    </div>
  </div>
</section>

<style>
  .problem {
    padding: var(--space-4xl) 0;
    background: var(--color-bg);
  }

  .problem-content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-3xl);
  }

  .problem-content h2 {
    margin-bottom: var(--space-lg);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
  }

  .problem-text {
    font-size: 1.125rem;
    color: var(--color-text-light);
    margin: 0;
  }

  .stats-row {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    max-width: 900px;
    margin: 0 auto;
    padding: var(--space-2xl);
    background: var(--color-bg-alt);
    border-radius: var(--radius-xl);
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-secondary);
    margin-bottom: var(--space-xs);
  }

  .stat-label {
    font-size: 0.9375rem;
    color: var(--color-text-light);
  }

  .stat-divider {
    display: none;
  }

  @media (min-width: 768px) {
    .stats-row {
      flex-direction: row;
      align-items: center;
      justify-content: center;
      gap: var(--space-2xl);
    }

    .stat-divider {
      display: block;
      width: 1px;
      height: 3rem;
      background: var(--color-border);
    }
  }
</style>

---
---

<section class="solutions" id="how-it-works">
  <div class="container">
    <div class="solutions-header">
      <h2>What we handle for you</h2>
      <p>So your team can focus on the guests in front of them.</p>
    </div>

    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
          </svg>
        </div>
        <h3>Every call answered</h3>
        <p>First ring. Every time. No exceptions.</p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
            <line x1="16" y1="2" x2="16" y2="6"/>
            <line x1="8" y1="2" x2="8" y2="6"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
          </svg>
        </div>
        <h3>Reservations managed</h3>
        <p>Book, modify, cancel. Automatically.</p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>
            <line x1="12" y1="17" x2="12.01" y2="17"/>
          </svg>
        </div>
        <h3>Questions answered</h3>
        <p>Hours, menu, parking, directions.</p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
        </div>
        <h3>After-hours coverage</h3>
        <p>Closed? We're still taking reservations.</p>
      </div>
    </div>

    <div class="demo-cta">
      <p>Reach out now and receive a live demo within hours.</p>
      <a href="#contact" class="btn btn-secondary">Book a Demo</a>
    </div>
  </div>
</section>

<style>
  .solutions {
    padding: var(--space-4xl) 0;
    background: var(--color-bg-alt);
  }

  .solutions-header {
    text-align: center;
    max-width: 500px;
    margin: 0 auto var(--space-3xl);
  }

  .solutions-header h2 {
    margin-bottom: var(--space-sm);
  }

  .solutions-header p {
    color: var(--color-text-light);
    font-size: 1.125rem;
    margin: 0;
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    max-width: 900px;
    margin: 0 auto var(--space-3xl);
  }

  .feature-card {
    background: white;
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    text-align: center;
    transition: all var(--transition-base);
  }

  .feature-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
  }

  .feature-icon {
    width: 3rem;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-md);
    background: rgba(99, 102, 241, 0.1);
    border-radius: var(--radius-lg);
    color: var(--color-secondary);
  }

  .feature-card h3 {
    font-size: 1.125rem;
    margin-bottom: var(--space-xs);
  }

  .feature-card p {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    margin: 0;
  }

  .demo-cta {
    text-align: center;
    padding: var(--space-2xl);
    background: white;
    border-radius: var(--radius-xl);
    max-width: 500px;
    margin: 0 auto;
  }

  .demo-cta p {
    color: var(--color-text-light);
    margin-bottom: var(--space-lg);
  }

  @media (min-width: 768px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(4, 1fr);
    }

    .feature-card {
      text-align: left;
    }

    .feature-icon {
      margin: 0 0 var(--space-md);
    }
  }
</style>

---
---

<section class="final-cta" id="contact">
  <div class="container">
    <div class="cta-content">
      <h2>Find Out How Much Revenue You're Losing</h2>
      <p>
        Get a free analysis of your missed call rate and see exactly what it's costing you.
        No obligation, no pressure—just the numbers.
      </p>

      <div class="cta-buttons">
        <a href="#" class="btn btn-white btn-lg">
          Book a Free 15-Min Strategy Call
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>

      <div class="cta-extras">
        <div class="extra-item">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>Free call-loss audit</span>
        </div>
        <div class="extra-item">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>No credit card required</span>
        </div>
        <div class="extra-item">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>15-minute call, max</span>
        </div>
      </div>
    </div>

    <div class="cta-visual">
      <div class="contact-card">
        <h3>Or reach out directly</h3>
        <div class="contact-methods">
          <a href="mailto:hello@msnai.ca" class="contact-method">
            <div class="method-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
            </div>
            <div class="method-info">
              <strong>Email</strong>
              <span>hello@msnai.ca</span>
            </div>
          </a>
          <a href="tel:+14165551234" class="contact-method">
            <div class="method-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
              </svg>
            </div>
            <div class="method-info">
              <strong>Phone</strong>
              <span>Coming Soon</span>
            </div>
          </a>
        </div>
        <p class="contact-note">
          We typically respond within 2 hours during business hours.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    padding: var(--space-4xl) 0;
  }

  .final-cta .container {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  .cta-content h2 {
    color: white;
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: var(--space-lg);
  }

  .cta-content > p {
    font-size: 1.125rem;
    opacity: 0.9;
    margin-bottom: var(--space-xl);
    max-width: 500px;
  }

  .cta-buttons {
    margin-bottom: var(--space-xl);
  }

  .cta-extras {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .extra-item {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: 0.9375rem;
    opacity: 0.9;
  }

  .extra-item svg {
    color: var(--color-success);
  }

  .contact-card {
    background: white;
    border-radius: var(--radius-xl);
    padding: var(--space-xl);
    color: var(--color-text);
  }

  .contact-card h3 {
    font-size: 1.25rem;
    margin-bottom: var(--space-lg);
    color: var(--color-text);
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .contact-method {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md);
    background: var(--color-bg-alt);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: all var(--transition-fast);
  }

  .contact-method:hover {
    background: var(--color-primary);
    color: white;
  }

  .contact-method:hover .method-icon {
    background: rgba(255, 255, 255, 0.2);
    color: white;
  }

  .contact-method:hover .method-info span {
    color: rgba(255, 255, 255, 0.8);
  }

  .method-icon {
    width: 3rem;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(26, 86, 219, 0.1);
    border-radius: var(--radius-md);
    color: var(--color-primary);
    transition: all var(--transition-fast);
  }

  .method-info {
    display: flex;
    flex-direction: column;
  }

  .method-info strong {
    font-size: 0.875rem;
  }

  .method-info span {
    font-size: 0.9375rem;
    color: var(--color-text-light);
    transition: color var(--transition-fast);
  }

  .contact-note {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    margin: 0;
    text-align: center;
  }

  @media (min-width: 640px) {
    .cta-extras {
      flex-direction: row;
      flex-wrap: wrap;
      gap: var(--space-lg);
    }
  }

  @media (min-width: 1024px) {
    .final-cta .container {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
